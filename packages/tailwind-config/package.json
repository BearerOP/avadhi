{
  "name": "@repo/tailwind-config",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for tailwind config'",
    "dev": "echo 'No dev needed for tailwind config'",
    "lint": "echo 'No lint needed for tailwind config'",
    "check-types": "echo 'No types to check for tailwind config'"
  },
  "exports": {
    ".": "./shared-styles.css",
    "./postcss": "./postcss.config.js"
  },
  "devDependencies": {
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.5"
  }
}
