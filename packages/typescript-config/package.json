{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for typescript config'",
    "dev": "echo 'No dev needed for typescript config'",
    "lint": "echo 'No lint needed for typescript config'",
    "check-types": "echo 'No types to check for typescript config'"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
